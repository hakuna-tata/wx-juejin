<view class="card" hover-class='hover-class' bind:tap="tapCardItem" data-url="{{card.url}}" data-open="{{card.open}}">
   <view class="title">
        <image src="{{card.image}}"/>
        <view>{{card.title}}</view>
   </view>
   <view class='count'>{{card.count}}</view>
</view>